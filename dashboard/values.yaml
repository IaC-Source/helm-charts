# applcation base
dashboard:
  printName: "Users"
  persistentVolume:
    enabled: true
    existingClaim: "dashboard"
  color: "blue"

service:
  port: 80
  targetPort: 80
  type: LoadBalancer

# pvc settings
persistence:
  size: 10Gi
